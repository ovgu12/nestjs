{
  "name": "nestjs-demo",
  "version": "1.0.0",
  "description": "POC nestjs with angular in a monorepo setup",
  "main": "index.js",
  "author": "Hoang Vu <hoangvuminh@hotmail.de>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "lint": "lerna run lint",
    "build": "lerna run build",
    "serve": "lerna run serve",
    "format": "lerna run format"
  },
  "engines": {
    "node": ">=20.10.0"
  },
  "devDependencies": {
    "lerna": "^8.0.1",
    "yarn-upgrade-all": "^0.7.2"
  }
}
