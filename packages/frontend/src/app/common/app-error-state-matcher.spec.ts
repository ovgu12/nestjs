import { AppErrorStateMatcher } from './app-error-state-matcher';

describe('AppErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new AppErrorStateMatcher()).toBeTruthy();
  });
});
